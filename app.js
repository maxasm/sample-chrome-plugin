// You can add all of the JS that interacts with the extension UI here
// Content Scripts -> Alter the site
// Service Worker -> The background scripts

// show informtion about downloads -> get information from the service worker
// cancel downloads -> send a message to the service worker
const h1 = document.querySelector("h1")
